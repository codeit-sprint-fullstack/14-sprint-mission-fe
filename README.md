## 요구사항

### 기본 요구사항

#### 중고마켓
- [X] MongoDB에서 PostgreSQL을 사용하도록 코드를 마이그레이션합니다.

#### 공통
- [X] PostgreSQL을 사용합니다.
- [ ] 데이터 모델 간의 관계를 고려하여 `onDelete`를 설정합니다.
- [ ] 데이터베이스 시딩(seeding) 코드를 작성합니다.
- [X] 각 API에 적절한 에러 처리를 합니다.
- [X] 각 API 응답에 적절한 상태 코드를 리턴합니다.

---

### 자유게시판

#### Article 스키마
- [ ] `Article` 스키마를 작성합니다.
  - [ ] `id`, `title`, `content`, `createdAt`, `updatedAt` 필드를 가집니다.

#### 게시글 API
- [ ] **게시글 등록 API** — `title`, `content`를 입력해 게시글을 등록합니다.
- [ ] **게시글 조회 API** — `id`, `title`, `content`, `createdAt`를 조회합니다.
- [ ] **게시글 수정 API**
- [ ] **게시글 삭제 API**
- [ ] **게시글 목록 조회 API**
  - [ ] `id`, `title`, `content`, `createdAt`를 조회합니다.
  - [ ] offset 방식의 페이지네이션 기능을 포함합니다.
  - [ ] 최신순(`recent`)으로 정렬할 수 있습니다.
  - [ ] `title`, `content`에 포함된 단어로 검색할 수 있습니다.

---

### 댓글

- [ ] **댓글 등록 API** — `content`를 입력하여 댓글을 등록합니다.
  - [ ] 중고마켓 / 자유게시판 댓글 등록 API를 따로 만듭니다.
- [ ] **댓글 수정 API** — `PATCH` 메서드를 사용합니다.
- [ ] **댓글 삭제 API**
- [ ] **댓글 목록 조회 API**
  - [ ] `id`, `content`, `createdAt`를 조회합니다.
  - [ ] cursor 방식의 페이지네이션 기능을 포함합니다.
  - [ ] 중고마켓 / 자유게시판 댓글 목록 조회 API를 따로 만듭니다.

---

### 피드백 사항
- [X] /items/:id 라우트가 등록되지 않아 상세 페이지 이동이 정상적으로 이루어지지 않는 점
- [X] RegistrationPage가 <form> 대신 <div>로 작성되어 제출 기능을 충분히 활용하지 못한 점
- [X] 페이지네이션 UI가 아직 구현되지 않은 점
- [ ] Render 배포가 진행되지 않은 점